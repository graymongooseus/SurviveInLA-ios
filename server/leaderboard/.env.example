# Domain only, without https:// or a path. Point its DNS A record to your VPS.
LEADERBOARD_DOMAIN=rank.example.com
ACME_EMAIL=you@example.com
LOCAL_PORT=8080
# Optional IP HTTPS deployment using compose.ip-https.yaml and nginx-ip-challenge.conf:
# LEADERBOARD_IP=2.24.206.66
# COMPOSE_FILE=compose.yaml:compose.ip-https.yaml
